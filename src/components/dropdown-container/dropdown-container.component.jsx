import * as S from './dropdown-container.styles';

export default function DropdownContainer({ children }) {
   return (
      <S.DropdownContainer>
         {children}
      </S.DropdownContainer>
   )
}