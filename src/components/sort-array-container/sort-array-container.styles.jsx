import styled from "styled-components";
import { maxVal } from "../../routes/sorting/sorting-algorithms";

export const SortArrayContainer = styled.div`
   width: 100%;
   display: flex;
   align-items: flex-end;
   gap: .2rem;
   height: ${maxVal}rem;
`;